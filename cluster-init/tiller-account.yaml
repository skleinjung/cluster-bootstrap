apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app: helm
    name: tiller-bootstrap
  name: tiller-bootstrap
  namespace: kube-system
---
apiVersion: rbac.authorization.k8s.io/v1beta1
kind: ClusterRole
metadata:
  labels:
    app: helm
    name: tiller-bootstrap
  name: tiller-bootstrap
rules:
  - apiGroups: ['*']
    resources: ['*']
    verbs: ['*']
  - nonResourceURLs: ['*']
    verbs: ['*']
---
apiVersion: rbac.authorization.k8s.io/v1beta1
kind: ClusterRoleBinding
metadata:
  labels:
    app: helm
    name: tiller-bootstrap
  name: tiller-bootstrap
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: tiller-bootstrap
subjects:
  - kind: ServiceAccount
    name: tiller-bootstrap
    namespace: kube-system
