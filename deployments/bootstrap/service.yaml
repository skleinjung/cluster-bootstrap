---
# Source: flux/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: flux-master
  namespace: flux-master
  labels:
    app: flux
    chart: flux-0.9.2
    release: flux-master
    heritage: Tiller
spec:
  type: ClusterIP
  ports:
    - port: 3030
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: flux
    release: flux-master
